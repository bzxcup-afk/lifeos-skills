# 开发交付报告

## 基本信息

| 字段 | 内容 |
|------|------|
| 任务编号 | LIFEOS-20260405-002 |
| 任务名称 | 问题导向干预 - 资料补全与优先事项生成模块 |
| 执行日期 | 2026-04-05 |
| 执行人 | 智子 |
| 最终状态 | ✅ done |

---

## 1. 任务目标

在"问题导向干预入口"之后，继续完成：
1. 读取用户已有数据（profile、preferences、history、medication）
2. 生成关键问题并向用户提问（LLM动态生成，3-4个）
3. 接收用户回答
4. 结构化回答
5. 输出优先事项
6. 生成case文件

**范围限制（本次）：**
- 只实现 sleep-insomnia 样例
- 优先事项使用模板（后续可升级为LLM生成）
- case文件存储在本地，不接入飞书

---

## 2. 修改文件清单

| 文件路径 | 操作 | 说明 |
|---------|------|------|
| `skills/lifeos-main/scripts/intervention_data_collector.py` | 新增 | 资料补全核心模块 |
| `skills/lifeos-main/SKILL.md` | 修改 | 新增"## 8. 干预资料补全模块"章节 |
| `skills/lifeos-main/cases/` | 新增 | case文件存储目录 |

---

## 3. 核心功能说明

### 3.1 用户已有数据读取

从SQLite读取以下数据：

| 数据源 | 表名 | 读取内容 |
|--------|------|---------|
| 用户资料 | profiles | age, gender, height, weight, chronic_conditions, health_constraints |
| 用户偏好 | user_preferences | sports_likes/dislikes, lifestyle_constraints, execution_preferences |
| 历史记录 | daily_log | 最近14天睡眠、体重等 |
| 药品记录 | medication_supplement_list | 药品名称、剂量、频率 |
| 医疗记录 | medical_records | 最近90天相关记录 |

### 3.2 关键问题生成

**Prompt模板：**
```
你是一个问题干预流程助手。

当前问题：{display_name}

问题知识摘要：
{knowledge_content}

用户已有信息：
{user_known_data}

你的任务是：
根据当前问题知识和用户已有信息，找出"为了继续生成干预优先事项，还缺失的最关键问题"。

要求：
1. 只返回 3-4 个问题
2. 问题必须是高价值问题
3. 不要重复询问用户已有信息
4. 问题要自然、口语化
5. 优先问最能影响方案方向的信息
```

### 3.3 回答解析

基于规则提取（第一版），支持：
- 睡眠时间、起床时间
- 睡眠时长
- 主要问题类型（入睡难/易醒/早醒）
- 药物使用情况
- 白天困倦程度

### 3.4 优先事项格式

```
【本周重点（先做这3件事）】
1. ...
2. ...
3. ...

【可以尝试（可选）】
- ...

【这周只需要简单记录】
- ...
```

### 3.5 Case文件

存储路径：`cases/{problem_id}/case-{user_id}-{date}.md`

包含：
- 基本信息
- 知识文件路径
- 用户已有信息摘要
- 本轮补充提问
- 用户回答摘要
- 结构化回答（JSON）
- 当前优先事项

---

## 4. 自测结果

### 4.1 功能测试

```
输入：用户说"改善睡眠问题"，回答"我大概11点多睡，7点多起，睡6个多小时吧，主要是容易醒，白天还好不太困"

结果：
✅ canonical_id: sleep-insomnia
✅ 生成4个关键问题
✅ 解析出结构化数据：
   - sleep_duration: 6.0
   - main_problem: 容易醒
   - daytime_drowsiness: 不明显
✅ 生成3个优先事项 + 2个可选 + 2个记录项
✅ Case文件成功生成
```

### 4.2 Case文件内容验证

```
✅ 基本信息完整
✅ 用户已有信息（从preferences表读取）
✅ 本轮提问列表
✅ 用户回答摘要
✅ 结构化JSON
✅ 优先事项格式正确
```

---

## 5. 风险与边界

### 5.1 当前限制

- **数据库表不存在时**：静默失败，使用空数据继续流程
- **用户回答解析**：第一版使用规则提取，精度有限
- **优先事项**：第一版使用模板，后续可升级为LLM生成
- **多轮对话**：第一版只支持单轮，后续需要会话状态管理

### 5.2 待优化项

1. **LLM生成优先事项**：当前使用硬编码模板，应改为LLM动态生成
2. **回答解析升级**：第一版基于规则，建议升级为LLM解析
3. **会话状态管理**：多轮对话需要持久化session_data
4. **case文件更新**：同一个用户多次干预同一问题，应该更新而不是新建

---

## 6. 建议给测试Agent的验证点

### 6.1 功能验证

1. **数据读取验证**
   - 检查 profiles 表中的用户数据是否被正确读取
   - 检查 preferences 表中的偏好是否被正确读取

2. **问题生成验证**
   - 发送"改善睡眠问题"
   - 验证是否生成了3-4个问题
   - 验证问题是否与已有数据不重复

3. **Case文件验证**
   - 检查 `cases/sleep-insomnia/` 目录下是否生成了case文件
   - 检查case文件内容是否完整

### 6.2 异常验证

1. **用户拒绝回答**
   - 发送"不想回答"等
   - 验证是否给出温和的降级回复

2. **数据缺失**
   - profile为空的用户
   - 验证是否仍能继续流程

---

## 7. 阻塞事项

无阻塞事项。

---

## 8. 最终状态

**✅ done**

本次已完整实现资料补全模块：
- ✅ 用户已有数据读取
- ✅ 关键问题生成
- ✅ 用户回答接收与解析
- ✅ 优先事项输出
- ✅ Case文件生成
- ✅ SKILL.md已更新
